<div class="database-selector">
  <form>
    <select
      title="Selected database"
      name="database"
      hx-post="/schema/"
      hx-trigger="change"
      hx-indicator="#schema"
    >
      {{#each databases}}
        <option>{{this}}</option>
      {{/each}}
    </select>
  </form>
</div>